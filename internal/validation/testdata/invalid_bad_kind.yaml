pactoVersion: "1.0"

service:
  name: bad-enum
  version: 1.0.0

interfaces:
  - name: api
    type: http
    port: 8080

runtime:
  workload: service

  state:
    type: invalid
    persistence:
      scope: local
      durability: ephemeral
    dataCriticality: low

  health:
    interface: api
    path: /health
