name: CI
description: Shared CI steps — setup Go, lint, unit tests, e2e tests

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v6.3.0
      with:
        go-version: '1.25'

    - uses: actions/cache@v5.0.3
      with:
        path: |
          ~/.cache/go-build
          ${{ env.GOPATH }}/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download
      shell: bash

    - name: Check formatting
      run: test -z "$(gofmt -l .)"
      shell: bash

    - name: Run go vet
      run: go vet ./...
      shell: bash

    - name: Check cyclomatic complexity
      run: |
        go install github.com/fzipp/gocyclo/cmd/gocyclo@latest
        gocyclo -over 15 $(find . -name '*.go' ! -name '*_test.go' ! -path './vendor/*')
      shell: bash

    - name: Run linter
      uses: golangci/golangci-lint-action@v9.2.0

    - name: Check CLI docs are up to date
      run: |
        go run ./cmd/gendocs/
        git diff --exit-code docs/cli-reference.md
      shell: bash

    - name: Run unit tests
      run: go test ./...
      shell: bash

    - name: Run e2e tests
      run: go test -tags e2e ./tests/e2e/ -count=1 -timeout 120s
      shell: bash
