# PROOF - publish.mjs fail-closed refusals (each a distinct, tested code)

(b) duplicate-artifact-ownership:
  [duplicate-artifact-ownership] coordinate "ghcr.io/x/dash" is claimed by both "dashboard-image" and "other"

(c) unit-without-publisher:
  [unit-without-publisher] release unit "orphan" has no publisher (absent from release-plan publishOrder)

(d) publisher-without-unit (unresolved step):
  [publisher-without-unit] publishOrder step "core:bogus" does not resolve to any release unit
(d) publisher-without-unit (unit not in manifest):
  [publisher-without-unit] publishOrder step "core:go-module" -> unit "core" is not in the release manifest

(e) chart-appversion-mismatch (appVersion != image):
  [chart-appversion-mismatch] operator chart appVersion "4.7.1" != operator image version "4.7.0"

(f) dirty-tree:
  [dirty-tree] release tree is not clean (git status --porcelain is non-empty) — commit or stash first:
M integrations/kubernetes/go.mod

(guard) production-target-refused:
  [production-target-refused] refusing production-looking target "ghcr.io/trianalab/pacto-dashboard" — point PACTO_STAGING_REGISTRY at a disposable local registry
