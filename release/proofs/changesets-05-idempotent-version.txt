# PROOF 5 - idempotent version: second run with no new changesets makes no extra change

$ cat .changeset/zz-core.md
---
"@pacto/core": minor
---

test: idempotency

$ changeset version   # first run
🦋  All files have been updated. Review them and commit at your leisure

$ @pacto/core after first run:
2.8.0

$ changeset version   # second run (no changesets present)
🦋  warn No unreleased changesets found, exiting.

$ @pacto/core after second run (unchanged):
2.8.0
