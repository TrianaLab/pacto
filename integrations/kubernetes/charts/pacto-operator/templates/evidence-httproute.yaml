{{- if and .Values.evidence.enabled .Values.evidence.httpRoute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "pacto-operator.fullname" . }}-evidence
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pacto-operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: evidence
spec:
  {{- with .Values.evidence.httpRoute.parentRefs }}
  parentRefs:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.evidence.httpRoute.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if .Values.evidence.httpRoute.rules }}
  rules:
    {{- toYaml .Values.evidence.httpRoute.rules | nindent 4 }}
  {{- else }}
  rules:
    - backendRefs:
        - name: {{ include "pacto-operator.fullname" . }}-evidence
          port: {{ .Values.evidence.service.port }}
  {{- end }}
{{- end }}
